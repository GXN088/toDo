# toDo
Создайте метод с именем processValue, который принимает один аргумент:Создайте метод с именем processValue1.), который может быть разных типовОбъект (valueМетод должен использовать сопоставление шаблонов с instanceof для обработки значения и возврата строки на основе этих правил:Если это Integer: вернуть "Number: " за которым следует удвоенное значениеЕсли это String: вернуть "Text: " за которым следует значение в верхнем регистреЕсли это Boolean: вернуть "Boolean: " за которым следует противоположное значение (true становится false и наоборот)Для любого другого типа: вернуть "Unknown"
